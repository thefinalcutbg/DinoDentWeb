---
permalink: /rqlite/
title: "rqlite сървър (по желание)"
layout: splash
---

<br>

## rqlite сървър (по желание)

Тази страница е по желание и е за потребители, които искат да използват мрежова база данни с DinoDent чрез **rqlite**.

👉 Официален сайт на rqlite: https://rqlite.io

Този файл служи **само като въвеждащо ръководство** за базова работа с rqlite в контекста на DinoDent.  
За пълните възможности, разширени настройки и актуална документация на rqlite, моля посетете официалния сайт.

Важно е да се знае, че **клиентът – т.е. програмата DinoDent – е тази, която създава базата данни**,  
грижи се за **структурата ѝ**, **нейното обновяване** (миграции) и съвместимостта ѝ с текущата версия на програмата.  
Потребителят **не е необходимо** ръчно да създава таблици или да поддържа структурата на базата данни.

Ако не знаете какво е това или не ви трябва – спокойно можете да го пропуснете. DinoDent работи напълно нормално и без rqlite.

## Какво е rqlite (накратко)

rqlite е сървър, който позволява SQLite база данни да работи през мрежата.

Използва се ако:
- базата данни трябва да е на друг компютър
- няколко компютъра трябва да работят с една и съща база

## 1. Най-лесен вариант – локално стартиране (само на този компютър)

Това е препоръчителният вариант за първо пробване.

1) Отворете папката с rqlite (там, където е rqlited.exe).
2) Отворете Command Prompt (cmd) в тази папка.
3) Напишете:

rqlited data

Сървърът вече работи само на този компютър.

## 2. Стартиране в локална мрежа (LAN / Wi-Fi)

Използва се, ако друг компютър в същата мрежа трябва да се свърже.

## 2.1 Намиране на IP адреса на компютъра

1) Натиснете Start  
2) Напишете cmd и натиснете Enter  
3) Напишете:

ipconfig

Потърсете ред от вида:

Wireless LAN adapter Wi-Fi:  
IPv4 Address : 192.168.0.110

Запомнете IP адреса (например 192.168.0.110).

## 2.2 Разрешаване на достъп през защитната стена (Firewall)

Вариант 1 – достъп от цялата мрежа:

netsh advfirewall firewall add rule name="rqlite HTTP 4001" dir=in action=allow protocol=TCP localport=4001

Вариант 2 – достъп само от локалната мрежа (по-сигурно):

netsh advfirewall firewall add rule name="rqlite HTTP 4001 LAN" dir=in action=allow protocol=TCP localport=4001 remoteip=192.168.0.0/24 profile=private

## 2.3 Стартиране на rqlite за LAN

rqlited.exe -http-addr 0.0.0.0:4001 -http-adv-addr 192.168.0.110:4001 data

## 3. Зареждане на съществуваща база данни

rqlite -H 127.0.0.1 -p 4001  
.restore C:\път\до\database.db

## 4. Добавяне на потребител и парола (препоръчително)

Създайте файл auth.json със съдържание:

{
  "users": [
    {
      "username": "admin",
      "password": "password123",
      "perms": ["all"]
    }
  ]
}

Стартиране:

rqlited.exe -auth auth.json data

## 5. HTTPS (по-сигурна връзка)

rqlited.exe -http-addr 0.0.0.0:4001 -http-adv-addr 192.168.0.110:4001 -http-cert server.crt -http-key server.key data

## 6. Най-висока сигурност (mTLS – за напреднали)

rqlited.exe -http-addr 0.0.0.0:4001 -http-adv-addr 192.168.0.110:4001 -http-cert server.crt -http-key server.key -http-ca-cert ca.crt -http-verify-client data
